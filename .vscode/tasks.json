{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/BlazorUI.Server/BlazorUI.Server.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}